# Optional. If set, the "Ask My AI Portfolio Assistant" widget (app/api/chat/route.ts)
# calls the Claude API grounded in your portfolio data. If unset, the widget falls
# back to local keyword-matched answers from data/portfolio.ts (assistantFaq) —
# no key required to run the site.
ANTHROPIC_API_KEY=
